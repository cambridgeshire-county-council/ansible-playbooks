---
  - name: Configure banner playbook
    hosts: switches
    tasks:
    - name: Configure banner task
      cisco.ios.ios_banner:
        banner: login
        text: "Example banner text"
        state: present
